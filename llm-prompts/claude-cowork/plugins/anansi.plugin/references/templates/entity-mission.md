---
entity_type: mission
template_class: identity
atomic: true
merge_strategy: container
template_version: "1.0"
description: "A Coruscant mission — a named objective executed by a coordinated fleet of drone flights"

atomic_criteria: >
  Represents one top-level mission record with a defined objective and a set of
  operations/flights that execute to accomplish it. A mission has a clear goal
  statement, a Pilot (the operator directing it), and a source_id that ties all
  flight outputs back to this mission record.

identity_fields:
  name:
    type: string
    required: true
  goal:
    type: string
    required: true
    description: "The specific objective this mission is working toward. Verb-noun phrase that can be marked done."
  status:
    type: string
    description: "Active, completed, on-hold, cancelled."
  pilot:
    type: string
    description: "The human operator directing this mission."
  manifest_path:
    type: string
    description: "Path to the mission manifest YAML file on disk."
  summary:
    type: string
    description: "One-paragraph source-agnostic description of what this mission does and why."
  content:
    type: string
    format: prose
    description: "2–4 paragraphs: what the mission is trying to achieve, why it matters, how it is structured, and what it found."

roster_sections:
  operations:
    source_field: operations
    render_as: "## Operations"
    row_format: "- [[-{slug}|{name}]] — {description}"
    dedupe_by: [slug]

sources:
  generic_prose:
    hint: "Mission records are typically generated by Coruscant at mission launch, not extracted from prose."
toc_structure: "none"
---
%%
field: name
description: Mission name (matches manifest `name` field)
%%
%%
field: goal
description: The specific objective this mission is working toward. Verb-noun phrase that can be marked done.
%%
%%
field: status
description: Current status (Active, Completed, On-hold, Cancelled)
%%
%%
field: pilot
description: The human operator directing this mission
%%
%%
field: manifest_path
description: Path to the mission manifest YAML on disk
%%
%%
field: summary
description: One-paragraph description of what this mission does and why it exists
%%
%%
field: content
description: Write 2–4 paragraphs on this mission. Cover its objective, how it is structured (operations and flights), why it matters, and what it found or is working toward. Write as a durable, source-agnostic knowledge entry.
%%
# {{name}}

## Identity
- Goal: {{goal}}
- Status: {{status}}
- Pilot: {{pilot}}
- Manifest: {{manifest_path}}

## Summary
{{summary}}

## Content
{{content}}

## Operations
