---
entity_type: operation
template_class: identity
atomic: true
merge_strategy: container
template_version: "1.0"
description: "A Coruscant operation — a named phase or parallel execution group within a mission"

atomic_criteria: >
  Represents one operation within a mission. An operation serves two purposes:
  (1) parallel execution — its child flights run concurrently; (2) context scoping —
  it groups related flights into a coherent phase with a shared objective.
  An operation has a name, a description of its scope, and belongs to a parent mission.

identity_fields:
  name:
    type: string
    required: true
  address:
    type: string
    description: "Top-level decimal address within the mission (e.g., '1', '2', '3')."
  description:
    type: string
    description: "What this operation is trying to accomplish — its scope within the mission."
  status:
    type: string
    description: "Active, completed, on-hold, cancelled."
  summary:
    type: string
    description: "One-paragraph description of this operation's scope and intent."
  content:
    type: string
    format: prose
    description: "2–4 paragraphs: what this operation covers, how its flights are structured, and what it contributes to the mission."

roster_sections:
  flights:
    source_field: flights
    render_as: "## Flights"
    row_format: "- [[-{slug}|{address} {name}]] — {description}"
    dedupe_by: [slug]

sources:
  generic_prose:
    hint: "Operation records are generated by Coruscant at mission launch, not extracted from prose."
toc_structure: "none"
---
%%
field: name
description: Operation name (matches manifest item title)
%%
%%
field: address
description: Top-level decimal address within the mission (e.g., '1', '2', '3')
%%
%%
field: description
description: What this operation is trying to accomplish — its scope within the mission
%%
%%
field: status
description: Current status (Active, Completed, On-hold, Cancelled)
%%
%%
field: summary
description: One-paragraph description of this operation's scope and intent
%%
%%
field: content
description: Write 2–4 paragraphs on this operation. Cover its scope, how its child flights are organized, why this grouping makes sense, and what the operation contributes to the mission.
%%
# {{name}}

## Identity
- Address: {{address}}
- Description: {{description}}
- Status: {{status}}

## Summary
{{summary}}

## Content
{{content}}

## Flights
