---
entity_type: coruscant_manifest
template_class: utility
atomic: true
merge_strategy: container
template_version: "2.0"
description: "A Coruscant flight manifest — a numbered listing of all flights within a mission, organized by operation. Grows as flights are launched and completed, each entry edged to its flight node."

identity_fields:
  name:
    type: string
    required: true
    description: "Manifest title, e.g. 'Mission Alpha Flight Manifest'"
  mission:
    type: string
    description: "The parent mission this manifest belongs to."
  generated_at:
    type: string
    description: "ISO-8601 timestamp when this manifest was generated."

roster_sections:
  flights:
    source_field: flights
    render_as: "## Flights"
    row_format: "- {address} [[-{slug}|{name}]] — {objective}"
    dedupe_by: [slug]

sources:
  generic_prose:
    hint: "Not produced by atomization — a manifest is generated by Coruscant at mission launch and grows as flights are completed."
toc_structure: "none"
---
%%
%%
field: name
description: Title of the manifest (e.g. "Mission Alpha Flight Manifest")
%%
%%
field: mission
description: The parent mission this manifest belongs to
%%
%%
field: generated_at
description: ISO-8601 timestamp when this manifest was generated
%%
# {{name}}

## Identity
- Mission: {{mission}}
- Generated: {{generated_at}}

## Flights
