---
entity_type: flight
template_class: identity
atomic: true
merge_strategy: pure_atomic
template_version: "1.0"
description: "A Coruscant flight — one atomic unit of drone work within an operation"

atomic_criteria: >
  Represents one flight: the smallest independently executable unit in a Coruscant
  mission. A flight has a single drone, a defined objective (the flight instructions),
  and a landing zone (the output file). One flight = one invocation of one drone.
  Flight records are source-bound — they capture metadata about the flight itself,
  not the content of the output. Output content lives in `flight-output` atoms.

identity_fields:
  name:
    type: string
    required: true
  address:
    type: string
    description: "Decimal address within the mission (e.g., '1.2', '2.3'). Three-part for sub-flights (e.g., '1.2.1')."
  drone:
    type: string
    description: "The Claude model or agent handling this flight."
  objective:
    type: string
    description: "What this flight was tasked with accomplishing — the core instruction."
  landing_zone:
    type: string
    description: "Output file path on disk. Matches hangar naming convention: {N}-{M}.md"
  status:
    type: string
    description: "Pending, active, landed, failed."
  summary:
    type: string
    description: "One-paragraph description of what this flight did and what it found."
  content:
    type: string
    format: prose
    description: "2–4 paragraphs: the flight's task, approach, and findings summary. Source-agnostic."

sources:
  generic_prose:
    hint: "Flight records are generated by Coruscant at flight launch, not extracted from prose."
toc_structure: "none"
---
%%
field: name
description: Flight name (matches manifest item title)
%%
%%
field: address
description: Decimal address within the mission (e.g., '1.2', '2.3', '1.2.1')
%%
%%
field: drone
description: The Claude model or agent handling this flight
%%
%%
field: objective
description: What this flight was tasked with accomplishing — the core instruction
%%
%%
field: landing_zone
description: Output file path on disk
%%
%%
field: status
description: Flight status (Pending, Active, Landed, Failed)
%%
%%
field: summary
description: One-paragraph description of what this flight did and what it found
%%
%%
field: content
description: Write 2–4 paragraphs on this flight. Cover what it was tasked with, how it approached the task, and what it found or produced. Write as a durable, source-agnostic knowledge entry.
%%
# {{name}}

## Identity
- Address: {{address}}
- Drone: {{drone}}
- Objective: {{objective}}
- Landing Zone: {{landing_zone}}
- Status: {{status}}

## Summary
{{summary}}

## Content
{{content}}
